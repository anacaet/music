= form_for @album do |f|
  - if @album.errors.any?
    #error_explanation
      %h2= "#{pluralize(@album.errors.count, "error")} prohibited this album from being saved:"
      %ul
        - @album.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :year
    = f.date_select :year
  .field
    = f.label :genre
    = f.text_field :genre
  .field
    = f.label :data
    = f.text_field :data
  .field
    = f.label :artists_id
    = f.number_field :artists_id
  .actions
    = f.submit 'Save'
